export default function SocialButtons() {
  return (
    <div className="flex gap-3 mt-3">
      <button className="flex-1 py-2 rounded-lg bg-[var(--card-bg)] hover:bg-[var(--card-bg)]">Continue with Google</button>
      <button className="flex-1 py-2 rounded-lg bg-[var(--card-bg)] hover:bg-[var(--card-bg)]">Continue with GitHub</button>
    </div>
  );
}
